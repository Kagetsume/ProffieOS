# UI components (legacy folder)

Wiring UI lives in **`../elements/`** as Lit custom elements (`<po-*>`). This folder is unused — kept only as a pointer.

See [`../elements/README.md`](../elements/README.md).
