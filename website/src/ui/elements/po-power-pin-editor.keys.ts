/**
 * i18n keys for {@link PoPowerPinEditor}.
 *
 * @module ui/elements/po-power-pin-editor.keys
 */
import { commonKeys } from '../../i18n/common-keys.js';

export const powerPinEditorKeys = {
  remove: commonKeys.actions.remove,
  heading: 'heading',
  hint: 'hint',
  addPowerPin: 'addPowerPin',
} as const;
