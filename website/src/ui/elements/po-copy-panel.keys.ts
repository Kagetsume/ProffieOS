/**
 * i18n keys for {@link PoCopyPanel}.
 *
 * @module ui/elements/po-copy-panel.keys
 */
import { commonKeys } from '../../i18n/common-keys.js';

export const copyPanelKeys = {
  copy: commonKeys.actions.copy,
  copied: commonKeys.actions.copied,
  download: commonKeys.actions.download,
} as const;
