/**
 * Saber hilt SVG asset path (served from `website/public/`).
 *
 * @module preview/hilt-asset
 */

/** User-provided vector hilt (may be renamed to `saber-hilt.svg` later). */
export const HILT_SVG_URL = '/saber-hilt.svg.svg';
